# Dependencies
node_modules/

# Build output (built fresh by `prepare`/`prepack` on install & publish)
dist/
*.tsbuildinfo
dist/.gitignore

# Local secrets & config — never commit SendKeys
.env
.env.*
*.local.json
config.local.*
.donechan.local.toml

# OS noise
Thumbs.db
Desktop.ini
.DS_Store

# Editor
.idea/
.vscode/*
!.vscode/extensions.json

# ZCode / agent workspace configs created automatically in this repo
.zcode/
